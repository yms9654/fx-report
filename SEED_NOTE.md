# history.json 시드에 대해

`history.json` 은 52주 고저 계산용 누적 파일이다. 네이버 API가 40영업일치만 주기 때문에
매 실행마다 병합하고, 365일 지난 값은 버린다.

최초 시드로 **2026-04-14 = 1562.47** 한 건만 넣었다. 이 값은 2026-08-26 시점
Investing.com이 표기한 52주 고점이며, 날짜는 정확한 기록일이 아니라 근사치다.
52주 고점 표시를 유지하기 위한 자리표시자이고, 2027-04 경 자동으로 만료된다.

그 외 과거 종가는 시드하지 않았다. 실행이 쌓이면서 실제 수집분으로 채워지고,
그때부터 52주 범위는 전부 실측값으로 계산된다.
